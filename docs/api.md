# API Reference

> Auto-maintained by the Developer agent. Do not edit manually.
> Updated after every feature that adds or changes an endpoint.

> **Note (2026-06-11):** The previous contents of this file documented a different
> project ("Claude Learning" / CDE Dashboard portal) and were carried over as
> boilerplate when this repo was scaffolded. That content has been removed and is
> recoverable from git history if needed.

---

## Endpoints

MMG Explorer currently has no API endpoints. The solution is a single Blazor Server
project (`src/MmgExplorer`); no Api project exists yet. When the first Minimal API
feature is implemented (per the conventions in `CLAUDE.md` — endpoint groups in
`Endpoints/[Feature]Endpoints.cs`, registered via `app.Map[Feature]Endpoints()`),
document each endpoint here.

| Method | Route | Description | Spec |
|---|---|---|---|
| _none yet_ | | | |
